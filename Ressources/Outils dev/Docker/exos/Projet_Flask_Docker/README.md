# Dockerisation du projet Flask

## Commande

`$ docker-compose up`

Attention à bien avoir les fichiers des modèles `mnist_rf.sav` et `mnist_cnn.h5` dans le répertoire. Sinon le conteneur ne démarrera pas...
